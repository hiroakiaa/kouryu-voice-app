# 応答後の接続表示と高速化

## 変更した仕組み

着信の「応答」を押した直後から、相手の音声が実際に届くまで全画面の接続表示を維持します。背後では、着信の確定、参加情報の確認、マイク取得、TURN設定、WebRTC接続を進めます。マイク取得とTURN設定は並行して開始し、順番待ちを減らしました。発信側も相手の応答を検知した時点で同じ表示へ切り替わります。

接続済みの判定は画面遷移ではなく、相手の音声トラックを受信した時点です。音声が届いたら完了テキストを挟まず、全画面表示を滑らかに閉じて通話画面へ移ります。失敗時は全画面表示を閉じ、通常のエラー案内へ戻します。自動再接続の既存処理と、接続段階の匿名タイムライン記録は維持しています。Service Workerの更新は通話中やモーダル表示中には再読み込みせず、終話後に反映します。

## 料金への影響

画面表示、マイクの事前準備、接続時間の端末内記録には外部API料金はかかりません。TURN資格情報の取得回数、Firestoreの参加・接続データ、音声中継の課金方式も変更していないため、10分通話の概算料金は従来と同じです。マイク準備を早めても録音や音声保存は行いません。

## 確認項目

- 応答直後に着信画面から全画面接続表示へ切り替わる
- 準備中の参加ボタンや電話画面の変化が露出しない
- 相手の音声受信後、完了テキストを挟まず通話画面へ移る
- 接続失敗時に操作可能な画面へ戻る
- 通話中のアプリ更新で強制再読み込みしない
- iPhoneとPCの実機では、通知タップ、応答、初回音声、終話、再発信を連続して確認する

## 使い方のヒント

`callTips` に、応答後は全画面案内のまま待てば参加確認・マイク・音声接続が自動で進み、準備後はそのまま通話画面へ切り替わることを記載しました。あわせて、管理者メニューの料金表示と、通話時間に上限がない現在の自動退室条件へ説明を修正しました。今後は機能更新ごとに `callTips` の内容も同期して確認します。
