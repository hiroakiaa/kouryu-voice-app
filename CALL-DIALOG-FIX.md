# 発信時の画面停止 修正

## 変更内容

電話番号入力後の発信ボタンと、履歴からの再発信で、確認画面が見えないまま背景だけが操作不能になる問題を修正しました。確認画面を中央に表示し、キャンセル・発信を選べます。連続操作でも確認画面を二重に開きません。

## 原因と仕組み

発信確認ダイアログに、旧着信通知の `display: none` が適用されていました。ブラウザはモーダル背景を開いていましたが、本体が非表示だったためフリーズしたように見えていました。開いている電話用ダイアログを明示的に表示するCSSへ修正しています。

## 費用への影響

画面表示だけの修正で、Cloudflare Workers AI、Realtime、Firestoreの呼び出し回数は増えません。実際の請求額と利用相当額への追加影響は0円です。

## 確認

番号入力と履歴の両方が使う共通発信処理に対し、確認画面が表示されることと二重表示を防ぐことを自動テストに追加しました。root版と `voice-standalone` 版の一致も確認しています。
