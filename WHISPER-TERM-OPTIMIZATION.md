# Whisper発話検出・共有用語AI

## 変更内容

- 通話音声は従来どおりWebRTCで送り、AI補助の失敗や待ち時間が通話を止めない構成を維持した。
- 補助音声認識はCloudflare Workers AIの`whisper-large-v3-turbo`を使用する。
- 端末内で無音を除き、自然な発話終了または最長8秒で音声を区切る。
- 発話の先頭を欠かさないよう約0.4秒の先行音声を保持し、8秒で強制分割した場合だけ約0.4秒を重ねる。
- 登録済み用語をWhisperの文脈ヒントへ渡し、API、SVG、RSSなどの表記を認識しやすくした。
- 登録済み用語は端末内辞書で照合する。未知候補だけを15秒単位でまとめてAIへ送る。
- 同一の認識文が複数端末から同時に届いた場合、Durable Objectで5分間統合し、AI処理を1回にする。認識文そのものは保存しない。
- 通常の用語説明も、たとえ話と同じように全利用者で共有する。同じ用語の2回目以降は保存済み説明を返す。
- 今回の通話料金では、実際にWhisperへ送った秒数、用語抽出回数、審査回数、説明生成回数、たとえ生成回数、Firestore処理、TURN通信量を合算する。
- 共有済みの通常説明とたとえは生成費を0円として集計する。
- 10分試算は、2人7分、3人8分、4人9分の合計発話、未知候補のAI補助12回、説明5回、新規たとえ2回を多めの条件として表示する。

## 処理の流れ

1. 各端末は自分のマイク音声だけを監視する。
2. 発話区間だけを16kHz・モノラルPCMへ変換する。
3. Whisperが文字を確定する。
4. 登録済み用語を端末内で照合し、すぐ用語一覧へ加える。
5. 残った未知候補を15秒単位でまとめる。
6. Workerが同一要求を統合し、必要な場合だけ用語抽出と登録審査を行う。
7. 採用した一般的な専門用語だけを共有辞書へ保存する。
8. 用語が押されたとき、共有済み説明を返す。未作成の場合だけAIが生成して共有保存する。

## 料金への影響

単価は2026年9月4日に確認したCloudflare公開料金、1ドル150円で概算する。Whisper large-v3-turboは音声1分あたり約0.00051ドル、Workers AIは1日10,000ニューロンまで無料である。実際の請求額はアカウント全体の無料枠、発話量、再試行、共有済みデータによって変わる。

10分試算の標準的な範囲は次のとおり。

| 参加人数 | 直接接続 | 全通信がTURNの場合 |
| ---: | ---: | ---: |
| 2人 | 約0.71〜1.36円 | 約0.85〜1.50円 |
| 3人 | 約0.78〜1.44円 | 約1.22〜1.87円 |
| 4人 | 約0.86〜1.51円 | 約1.72〜2.38円 |

下限は登録済み用語中心、上限は未知候補のAI補助12回、説明5回、新規たとえ2回を含む。携帯回線の通信料は含まない。

「今回の通話」は実測した利用量を集計するため、発話しなかった時間や共有説明の再利用を反映する。終了後の履歴にも通話全体の概算額と10分換算額を保存する。複数端末の最終パケットが切断前に届かなかった場合や、Cloudflare側で失敗後も処理が完了した場合には小さな誤差があり得る。

## 保存とプライバシー

- 音声と認識文は保存しない。
- 同一要求の統合には認識文から作ったSHA-256識別子を使い、保存するのは採用用語と有効期限だけである。
- 共有保存するのは一般的な用語、説明、たとえであり、利用者IDや会話全文を含めない。
- 電話番号、メールアドレス、URLは未知候補をAIへ送る前に除去する。

## 確認項目

- 44.1kHzと48kHzの入力を16kHzへ変換できること。
- 無音、一定音、単発クリックをWhisperへ送らないこと。
- 発話を8秒以内に分割し、強制分割時だけ前後を重ねること。
- 認識ヒントの不正な値を除外すること。
- 同一認識文の同時要求が1回のAI生成になること。
- 通常説明とたとえが利用者間で共有されること。
- 共有された生成結果を今回の通話料金から除外すること。
- rootと`voice-standalone`の配布ファイルが一致すること。

## 参照した料金表

- https://developers.cloudflare.com/workers-ai/platform/pricing/
- https://developers.cloudflare.com/workers-ai/models/whisper-large-v3-turbo/
- https://developers.cloudflare.com/realtime/turn/faq/
