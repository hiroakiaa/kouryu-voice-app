# たとえ話の作り直し機能の廃止

更新日：2026-09-03

## 変更と仕組み

「問題を報告して作り直す」は、表示中のたとえを問題ありとして扱い、AIに再生成を要求し、作り直した内容を共有辞典へ保存する機能でした。今回、このボタンと補足文、画面側の再生成処理を削除しました。APIも作り直し指定を拒否するため、更新前の画面から要求されてもAIを実行しません。

「たとえを見る」は継続します。同じ用語・ジャンルの保存済みのたとえがあれば共有データを返し、未登録の場合にAIで作成して保存します。同時に要求されても生成を重複させない仕組みも継続します。過去に問題ありと記録された未修復データについてだけは、従来の復旧処理を残しています。

## 費用

今回の廃止により、新しいAI利用は増えません。利用者による作り直しのAI生成費が発生しなくなります。削減額は、実際に何回作り直していたかによって変わるため、今回の削減額は測定していません。

音声認識・用語検出・通常の説明・初回のたとえ生成・共有データの取得の費用は従来どおりです。料金画面の10分試算は利用者の作り直し回数を加算していなかったため、表示額は変わりません。試算は無料枠適用前の利用相当額で、実際の請求額ではありません。実請求額は今回取得していません。

## 確認

自動テスト85件成功。保存済みのたとえが再起動後も再利用されること、作り直し指定がAPIと共有キャッシュで拒否されAIが呼ばれないこと、初回生成・キャッシュ・退室時の中断を確認しました。rootとvoice-standaloneの配布ファイルを同期しています。
