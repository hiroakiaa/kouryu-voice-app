# 発信と連絡を迷わないための改善

## 変えた内容

発信確認で「電話する」を押した直後に呼び出し準備画面を表示します。番号入力画面へ一瞬戻って見える時間をなくし、接続確認中は取消ボタンを無効にします。発信情報の作成後に通常の呼び出し中表示と取消操作へ切り替わります。通信や番号確認に失敗した場合だけ準備画面を閉じ、理由を表示します。

電話帳、履歴、グループのカードに「連絡」を追加しました。押すと新しい連絡の宛先が選択済みで開きます。履歴だけに残る未登録番号は、番号の所有者を確認してから宛先にします。グループは自分を除く現在のメンバーを宛先にします。

在席状態は、電話帳または連絡画面を表示している間、対象ユーザーの状態をリアルタイムに購読します。画面を離れた相手の購読は解除し、不要な読み取りを増やしません。従来の初回取得と期限切れ判定も残すため、再表示時にも状態を補正できます。

新しい連絡の対応時期は「指定なし」「すぐ対応」「午前」「午後」「時間帯」をボタンで選べます。「時間帯」は開始と終了を指定でき、どの選択にも短い補足を加えられます。はてなボタンには各選択肢の意味を表示します。送信中は送信ボタンを無効にし、ボタン内を「送信中」の回転表示へ変えて二重送信を防ぎます。

## 保存する情報

期限を指定した連絡には、終了時刻、必要に応じた開始時刻、選択肢の名前、40文字以内の補足を保存します。Firestoreの検証規則でも型、文字数、開始と終了の順序を確認します。

## 費用への影響

発信直後の画面表示と送信ローディングは端末内の表示変更だけなので追加料金はありません。在席状態は、電話帳または連絡画面を開いている間に相手の状態が変わった回数だけFirestoreの読み取りが増えます。対象外の画面では購読を解除します。カードから連絡を送る処理は既存の連絡送信と同じ保存・Push通知を使うため、新しい外部サービス費は発生しません。

## 確認項目

- 発信確定直後、通信待ちでも番号入力画面へ戻らない
- 電話帳、履歴、グループの連絡ボタンで宛先が選択済みになる
- 別端末で在席状態を変えると表示中の画面へ反映される
- 定型期限と任意の時間帯がカードへ正しく表示される
- 送信中にボタンを連打できず、完了後に元へ戻る
- 540px以下でも期限ボタンとカード操作が横にはみ出さない
