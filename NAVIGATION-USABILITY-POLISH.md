# 電話画面の操作性仕上げ

## 変更内容

- 上部4画面の選択状態を、白背景・緑枠・内側の縁取りで明確にしました。
- 下部の「電話」は電話機能全体、上部の「番号入力」は発信画面であることを読み上げ名でも区別しました。
- 履歴・電話帳・グループは、それぞれ独立したスクロール位置を記憶します。初めて開く画面は先頭から表示し、戻った画面は以前の位置へ戻ります。
- 電話帳とグループの最下部に追加ボタン用の安全な余白を確保しました。
- 自分番号の詳細に画面高に応じた最大高と内部スクロールを設定しました。
- 上部4ボタンを左右矢印、Home、Endキーで切り替えられるようにしました。

## 仕組み

画面切替時に現在のパネルの `scrollTop` を画面別に保存し、次のパネルを表示したあとにその画面固有の位置だけを復元します。上部ボタンはタブとして読み上げられ、選択中の項目に `aria-selected` と `aria-current` を設定します。追加ボタンの余白はスクロール領域内に持たせ、下部ナビゲーションとは別の領域として保ちます。

## 費用への影響

端末内の表示と操作だけの変更です。実際の請求額は0円、無料枠を除いた利用相当額も0円です。通信、Firestore、通知、音声中継、AI処理の追加利用はありません。

## 確認内容

- 390px幅で4ボタンが横一列に収まり横スクロールが発生しない
- 選択中の画面が視覚と読み上げの両方で分かる
- 各画面のスクロール位置が混ざらない
- 追加ボタンが最終カードを隠さない
- 自分番号の詳細が低い画面でも内部スクロールできる
- キーボードによる画面移動
- 発信・着信・終了と2端末相当の主要通話シミュレーション
- root版と `voice-standalone` 版の一致
