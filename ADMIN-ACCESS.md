# 管理者パスワード

管理者メニューは、入力された文字列をブラウザでSHA-256へ変換し、コード内の照合用ハッシュと比較します。元のパスワードはコードやデータベースへ保存しません。そのため、忘れたパスワードは復元せず、新しいパスワードのハッシュへ差し替えます。

認証に成功した端末では照合済みのハッシュをlocalStorageへ保存し、次回から入力を省略します。パスワードを変更すると古い照合値と一致しなくなるため、各端末で新しいパスワードを一度入力する必要があります。

この変更は外部APIやデータベースを使わないため、追加料金は0円です。
