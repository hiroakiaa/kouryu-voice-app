# iPhoneとPC間の通話経路修正

## 症状

着信へ応答すると両端末が通話画面へ移動しても、相手待ちまたは接続中のまま音声通話を始められない場合がありました。特にiPhoneと自宅PCのように、端末やネットワーク条件が異なる組み合わせで起きやすい状態でした。

## 原因と変更

アプリはCloudflare TURNの短期接続情報を取得していましたが、自動参加の開始後に取得していました。そのためWebRTCのPeer接続が先に作られると、最初の接続候補がSTUNだけになり、直接接続できないネットワークでは音声経路が成立しませんでした。

追加の実機画像から、PCに表示された参加上限が「1対1」ではなく「最大4人」だったことも確認しました。これは1対1ルームへのURL移動が完了せず、元の画面だけが通話画面として表示されていた状態です。元画面を先に切り替える処理を廃止し、移動先をセッション内へ30秒間保持します。通常のURL移動、250ミリ秒後の確認、次回起動時の復旧の順で、必ず作成済みの1対1ルームを開き直します。

公開版を「通話だけ」モードでブラウザ実行した結果、料金表示が計測データの作成前に動き、JavaScriptが初期化途中で停止するエラーも確認しました。これが、URLを直接開いても「最大4人」「参加する」の初期表示から進まなかった直接の原因です。料金計測を先に作成してから通話だけモードの料金表示を描画する順序へ修正しました。

今回、次の順序へ変更しました。

1. Firebase認証を完了する
2. Cloudflare TURNの短期接続情報を取得する
3. 電話ルームへの参加権限を確認する
4. マイクと参加者情報を開始する
5. TURNを含む設定でWebRTC Peer接続を作る

TURN発行に失敗した場合は、従来どおりSTUNへ戻して接続を試します。取得処理は同じページ内で共有され、重複発行しません。

## 費用への影響

通話開始時にTURN接続情報の発行を1回行います。これは以前から行っていた処理を通話開始前へ移したもので、発行回数は増えません。直接接続できた通話はTURNの音声中継量が発生しません。直接接続できずTURNを利用した場合だけ、実際に中継した音声データ量がCloudflare Realtimeの利用量になります。

AI用語検出、音声認識、用語説明、たとえ生成の呼び出し回数と料金は変わりません。アプリの今回の通話の概算では、TURNが使われた場合だけ「音声の中継」へ反映します。無料枠内の実際の請求額は0円です。

## 確認結果

- 本番TURN発行エンドポイントがHTTP 201を返すこと
- 返却設定にSTUNとTURNの両方が含まれること
- 通話だけモードを公開ブラウザで開いて初期化エラーが発生しないこと
- 通話参加処理がTURN準備完了を待つこと
- 元画面では参加表示へ切り替えず、`n_`で始まる1対1ルームへ移動すること
- URL移動を取りこぼした場合に保存済みの移動先から復旧すること
- Peer接続がその後に作られること
- 発信側の応答検出と2端末参加シミュレーションを含む全テストが通過すること
- ルート版と配布版が一致すること

## 2026-09-05 退室操作と1対1表示

- 通話中は接続状態が失敗表示でも、退室ボタンが再接続へ分岐せず、必ずローカル終了処理を実行するようにした。再接続は専用ボタンで行う。
- 通話種別バッジを経過時間と同じ右側のまとまりに配置した。
- 1対1通話では2人のアイコンを中央に、同じ大きさと間隔で配置する。
- 追加費用: なし。画面と終了操作の変更で、AI・音声認識・TURNの追加利用はない。
- 確認: 全テスト成功、公開版で表示とブラウザエラーを確認する。

## 2026-09-05 終話音とバージョン表示

- 通話が実際に開始済みの場合だけ、終了時に短い下降音を1回再生する。自分・相手のどちらが終了しても同じ終話処理を通る。
- 音はブラウザ内のWeb Audio APIで生成し、音源ファイルや外部APIを使わない。
- versionPillは利用者向け画面から削除し、APP_VERSIONは内部診断と更新確認のため保持する。
- 追加費用: なし。通信やAI処理は増えない。

## 2026-09-05 着信画面の再設計

- 名前を最も大きくし、番号と通話種別を補助情報として整理した。
- 拒否と応答は赤・緑の円形電話アイコンにし、文字は白背景上へ分離して読みやすくした。
- モーダルの横幅・余白・ボタン間隔をスマートフォン向けに調整した。
- 追加費用: なし。CSSのみの変更で通信やAPI利用は増えない。

## 2026-09-05 発信確認の簡素化

- 「この相手に電話しますか？」を削除し、相手名を最も大きく表示する。
- 理解サポートあり・通常電話は小さなセグメント型トグルにまとめる。
- 電話する操作を大きな主ボタン、やめる操作を小さな補助ボタンにする。
- 追加費用: なし。HTML/CSSのみの変更で通信やAPI利用は増えない。

## 2026-09-05 発信確認モーダルの統一

- 通話種類トグルを左右同幅に固定し、ずれを解消した。
- サポート説明を丸いはてなボタン内へ隠し、必要なときだけ展開する。
- やめるボタンを廃止し、右上の閉じる・背景タップへ一本化した。
- モーダルを緑の枠、淡い背景、白文字の主ボタンでアプリ全体と統一した。
- 追加費用: なし。HTML/CSSと画面内操作のみで、API利用は増えない。

## 2026-09-05 発信ボタンの爽やかな配色

- 通話種類トグルと電話ボタンを280pxの同じ横幅に統一した。
- はてなボタンを30pxの真円にし、トグル右上へ少し重ねた。
- 電話ボタンを明るいミントグリーンのグラデーションにし、内側へ白い点線を追加した。
- 子要素へ直接指定されていた文字色より強く白を指定し、読みづらさを解消した。
- 追加費用: なし。CSSのみの変更でAPIや通信利用は増えない。

## 2026-09-05 発信トグルの整列

- fieldset内のlabelを同じ高さでセル全体へ伸ばし、上下の余白を統一した。
- 選択側の影を上下対称にして、下へずれて見える要因を除いた。
- 電話するボタンをヘッダーと同じ #efffd3 から #dff8bd の背景へ変更した。
- 追加費用: なし。CSSのみの変更。

## 2026-09-05 まとめられた履歴の削除

- 連続履歴カードの代表1件だけでなく、カード内にまとめた全履歴IDをFirestoreから削除する。
- 削除完了をawaitし、操作直後は一覧を先に更新する。失敗した場合は一覧を戻してエラーを表示する。
- 確認文をカード内の履歴すべてが対象だと分かる表現へ変更した。
- 追加費用: 削除1件につきFirestore delete 1回。既存履歴の削除操作時のみで、通常の通話料金は増えない。

## 2026-09-05 本文の丸文字

- 本文・ボタン・入力欄をZen Maru Gothicへ変更した。
- アプリ名とh1・h2・h3は従来のタイトル用フォントを明示して維持した。
- 既存のGoogle Fontsリクエストへ同梱し、CSSリクエスト数は増やしていない。
- 追加費用: なし。Google Fontsは無料で、アプリのAPI利用も増えない。

## 2026-09-05 グループカード操作の右寄せ

- グループ情報欄が残り幅を使い、group-row-actionsをmargin-left:autoでカード右端へ固定した。
- 狭い画面で折り返した場合も操作群を右寄せし、横スクロールは発生させない。
- 追加費用: なし。CSSのみの変更。

## 2026-09-05 グループ参加ボタン

- グループの参加ボタンを履歴の電話ボタンと同じ淡い背景、緑の枠、柔らかい影へ統一した。
- 最小横幅を108px、左右余白を18pxにして、詳細ボタンとの判別と押しやすさを改善した。
- 右端配置と狭い画面での折り返しは維持する。
- 追加費用: なし。CSSのみの変更。

## 参加者アイコンと状態表示の統一（2026-09-05）

- 自分を含む参加者アイコンを、アプリ共通の淡い黄緑グラデーション、緑枠、内側の点線、丸型に統一した。
- 黒い状態ラベルを廃止し、白・淡緑の小さな丸型バッジへ変更した。待機・接続・会話・マイクOFFは同じ形のまま色で見分けられる。
- 自分の状態文言を「自分・待機中」「自分・接続中」など、自然に読める表現へ整理した。
- 仕組み: 参加者の接続状態から作る既存の表示データはそのまま使い、表示層のCSSと短いラベルだけを変更している。通信処理への変更はない。
- 費用: CSSとブラウザ内表示だけの変更で、Cloudflare・AI・音声認識の追加料金は0円。

## グループ参加ボタンと自分表示の明色化（2026-09-05）

- グループ詳細の「通話に参加」を高さ62pxの主操作にし、淡い黄緑、緑枠、内側の点線、柔らかい影へ統一した。
- 通話中の自分の表示を横幅7remで揃え、アイコン面と状態バッジを同じ幅にした。
- 自分のアイコンと待機・接続・会話・マイクOFF表示を明るい黄緑・白系へ変更し、暗い背景を廃止した。
- 仕組み: 通話やグループ参加の処理は変更せず、既存ボタンと参加者状態にCSSを適用する。状態変化時は同じレイアウトのまま背景色だけが変わる。
- 費用: ブラウザ内のHTML/CSS表示だけの変更で、Cloudflare・AI・音声認識の追加料金は0円。

## グループ通話種類をトグル化（2026-09-05）

- グループ詳細モーダルの通話種類を、1本のセグメント型トグルへ変更した。
- 表示を「サポートあり」「サポートなし」に短縮し、選択側だけを白く浮かせて判別しやすくした。
- 通話参加ボタンより控えめな幅と高さにして、主操作との優先順位を明確にした。
- 仕組み: 既存のradio入力とグループ設定保存処理を維持し、マークアップのクラスとCSSだけでトグル表示にしている。
- 費用: 表示変更のみで、Cloudflare・AI・音声認識の追加料金は0円。

## 自分の参加者表示を右下へ固定（2026-09-05）

- 自分の表示を名前の頭文字から人物アイコンへ変更した。
- アイコン面を正方形の角丸にし、状態バッジも同じ横幅に揃えた。
- 自分の表示を参加者カードの右下へ固定し、相手の表示領域と重ならない余白を確保した。
- 仕組み: 自分だけ人物アイコンを描画し、既存の参加者ID判定を使って右下配置用CSSを適用する。相手側の頭文字と接続処理は変更しない。
- 費用: HTML/CSS表示のみの変更で、Cloudflare・AI・音声認識の追加料金は0円。

## サポート選択と説明UIの統一（2026-09-05）

- グループ通話モーダルと設定画面の選択肢を「サポートあり」「通常通話」に統一した。
- 両画面とも発信確認モーダルと同じセグメント型トグルと、右上に重なる小さな丸い「？」ボタンへ変更した。
- 「？」を押したときだけ、専門用語の検出・説明と通常通話の違いを表示する。
- 設定画面の説明には、ここで発信時の初期選択を決め、発信直前にも変更できることを記載した。
- 仕組み: 既存radio値と保存処理は維持し、共通のsetCollapsibleで説明を開閉する。グループモーダルを閉じると説明も閉じる。
- 費用: UIと端末内設定の変更だけで、Cloudflare・AI・音声認識の追加料金は0円。

## アプリ全体の整理・安定性確認（2026-09-05）

- 通話種類の名称を「サポートあり」「通常通話」に統一し、発信確認・設定・グループ・履歴・料金表示で同じ言葉を使うようにした。
- グループ詳細では招待操作を初期状態で隠し、「メンバーを招待」を押したときだけ滑らかに開くようにした。管理機能は従来どおり初期状態で隠す。
- 説明パネルは一度に1つだけ開くようにし、情報が重なって画面が長くなりすぎる問題を防いだ。
- 期限切れ・取消済みのグループ招待を一覧に表示しないよう整理した。
- 参加者を3〜4人表示する際の間隔と折り返しを調整し、自分の右下表示と重ならない領域を確保した。
- 670px以下ではグループモーダル、トグル、招待入力を画面幅内に収め、閉じる見出しをスクロール中も上部に残すようにした。
- 通話の発信・着信・応答・終話・再発信、通常通話のAI費0円、サポート費用の内訳、履歴削除、グループ参加人数の既存検証を再実行した。
- 仕組み: UIの開閉は共通のsetCollapsibleに集約し、期限判定は端末時刻と招待のexpiresAtを比較する。通話経路・WebRTC・料金計算式は変更していない。
- 費用: 画面整理と端末内判定のみで追加料金は0円。通常通話は引き続きAI処理を実行せず、サポートありの場合だけ利用分を計上する。

## 相手側の終話案内とグループモーダルの安定化（2026-09-05）

- 相手が1対1通話を終えた場合は相手名付き、グループから退室した場合は退室案内、グループ自体が終了した場合は終了案内をモーダルで約1.8秒表示する。
- マイク・音声・接続は終了通知を受けた時点で止め、通話画面だけを案内が消えるまで残す。案内はフェードしてから電話ホームへ戻る。
- グループ詳細を開く前に競合するダイアログを閉じ、`showModal()`でブラウザの最上位層へ表示する。古い画面構造が残った場合にも処理が止まらないよう、招待欄の要素確認と表示フォールバックを追加した。
- 費用: 端末内の表示制御だけで、Cloudflare・AI・音声認識の追加料金は0円。
