# アプリ更新後の画面復元

## 原因

Service Workerの更新が完了すると、安全に新しいHTML・JavaScript・CSSへ切り替えるため、アプリは一度だけ自動再読み込みします。以前は「履歴」を選んだ直後にこの切り替えが起きると、選択状態がURLへ保存されていなかったため、初期画面の「番号入力」に戻っていました。更新済みの2回目以降は再読み込みが起きないため、履歴を正常に表示できていました。

## 修正

連絡、番号入力、履歴、電話帳、グループを選ぶたびに、現在の画面をURLの `tab` パラメーターへ即時反映します。Service Workerによる再読み込み後はその値を読み、同じ画面を復元します。番号入力は既定画面なので、選択時には不要な `tab` パラメーターを取り除きます。

この処理はURLだけを書き換え、ページ移動や追加の通信を起こしません。したがって費用は増えません。ブラウザの戻る履歴も増やさないよう `replaceState` を使います。

## 確認

- 履歴を選ぶとURLに `tab=history` が反映される
- その状態で再読み込みしても履歴が選択されたままになる
- 電話帳、グループ、連絡も同様に復元される
- 番号入力へ戻ると `tab` 指定が削除される
