# 運用と料金監視

## Google Cloudの予算アラート

1. Google Cloud ConsoleでFirebaseと同じプロジェクトを開く。
2. 「お支払い」→「予算とアラート」→「予算を作成」を開く。
3. 対象プロジェクトを `test-project-579c6` に限定する。
4. 月額予算を設定し、50%、90%、100%でメール通知を有効にする。
5. Firestore Usage画面でreads・writes・deletesを週1回確認する。

予算アラートは利用を自動停止する機能ではありません。想定外の増加を早く発見するために使います。

## 4人通話の基準値

4台・10分の確認ごとに、次を記録します。

- estimatedReads
- writes
- deletes
- 1分あたりのreads / writes
- activeListenersNow
- 接続できたpeer数

大きく増えた場合は、アプリ内の自動ガードが同期頻度を抑えます。ユーザーには技術通知を出さず、内部診断だけに記録します。

## Firestore Rules

`firestore.rules` は通話用コレクション以外を拒否し、参加者・シグナルの項目、型、文字数を限定します。

現在のアプリにはログインがないため、Rulesだけで端末本人を完全に証明することはできません。匿名認証を導入するまでは、スキーマ外書き込みと無関係なコレクションへのアクセスを防ぐための保護として使用します。
