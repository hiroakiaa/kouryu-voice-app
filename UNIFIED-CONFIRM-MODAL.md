# 一時通知と確認画面の統一

## 変更内容

「通話が終了しました。」は参加者欄に重ならない小さな通知として約2.2秒表示し、フェードして消えるようにしました。通知は通常のレイアウトから外して配置するため、表示・非表示で参加者や他の要素は動きません。

履歴の1件削除・全削除を含め、ブラウザ標準の確認ポップアップをすべてアプリ内の共通モーダルへ変更しました。対象は電話帳削除、グループ削除・退出、メンバー退出、アカウント切替、通話からグループ画面への移動です。背景、閉じるボタン、取消ボタン、危険操作の色を統一しています。背景のタップとEscapeでも取り消せます。

## 費用と仕組み

表示と操作確認だけの変更です。確認後に実行されるFirestore操作の回数は従来と同じです。Cloudflare Workers AI、Realtime、Web Pushの使用量も増えません。実際の請求額と利用相当額への追加影響は0円です。

## 確認

アプリのソースにブラウザ標準 `confirm()` が残っていないこと、全操作が共通確認モーダルを使うこと、終了通知が絶対配置とフェード処理を使うことを自動テストで確認します。
