# アプリ情報のモーダル化

更新日：2026-09-03

appInfoTooltip をネイティブのモーダルダイアログへ変更しました。中央に情報を表示し、周囲を半透明のグレーで覆います。ほかの料金モーダルと同じ44pxの閉じるボタンを右上に配置しました。背景のタップ、Escキーでも閉じられます。開いている間のキーボード操作はダイアログ内に留まり、閉じると元のボタンへフォーカスが戻ります。料金画面を開く際はアプリ情報を閉じるため、背景だけが残ることを防ぎます。

表示・操作だけの変更です。AI呼び出し、API要求、データ保存、料金計算式の追加や変更はなく、この変更によるAI利用相当額の増加は0円です。実際の請求額は今回取得していません。

89件の既存テストが成功。ブラウザで背景と閉じるボタンの表示、閉じた後のフォーカス復帰、料金画面への切り替えを確認しました。iPhone実機での確認は行っていません。
