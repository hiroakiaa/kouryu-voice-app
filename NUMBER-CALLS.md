# 8桁の番号で電話・承認して追加（第1段階）

アプリ: https://hiroakiaa.github.io/kouryu-voice-app/?call=main-room

## 使い方
「番号で電話」→「番号を作る」で自分の8桁の番号を発行します。4桁ずつ表示し、コピーできます。相手は番号を入力して「電話する」を押します。
着信した持ち主が「応答する」を押すと2人の通話が始まります。同じ番号へ別の人が発信した場合、持ち主の「通話に追加」で現在の部屋へ参加します。持ち主を含め最大4人です。既存のURL参加・電話帳も利用できます。

## 承認と終了
承認前は番号通話の参加情報と音声接続用情報にアクセスできません。番号を知るだけ、通話URLをコピーするだけでは参加できません。
同時承認はFirestoreのトランザクションとルールで最大4人に制限します。ゲストは他の人を承認できません。ゲストが退室すると再参加には再承認が必要です。持ち主が退室すると部屋を終了します。
発信の取消、拒否、90秒の着信期限、同じ発信者の30秒以内の再発信制限、ブロックに対応しています。「番号の設定」から、この端末で最後にブロックした相手を解除できます。
承認後90秒たっても参加しなかった人、退出済み・4分以上更新のない人の枠は、次の承認時に持ち主側で回収します。部屋の権限自体にも期限があります。
別の人の通話・従来のURL通話に参加中の人は、その通話を退室してから番号着信に応答します。

## 保存場所と識別
Firestoreに、voiceNumbers（番号と所有者）、numberProfiles（本人の番号）、numberInvitations（発信状態）、numberVoiceCalls（承認メンバー・参加状態・接続情報）を保存します。音声や会話の認識文は、この機能では保存しません。
自分の番号は現在のFirebase匿名アカウントに結び付きます。同じブラウザーなら再読み込みしても使えます。現段階では別端末への引き継ぎ・ログインによる復元は未実装です。ブラウザーのデータ削除では失われます。通常の電話回線・SMSの番号ではなく、このアプリ内の番号です。
番号一覧の取得は禁止し、8桁を指定した検索だけを許可しています。着信の表示名は本人確認済みの名前ではありません。

## 費用
番号発行・検索・承認はAIを呼びません。Firestoreの少量の読み書きと、既存の着信通知を利用します。待機画面から音声認識を開始する変更はありません。

追加処理の比較用モデルとして、番号発行から追加承認まで通話全体60 reads・20 writes、既存アプリの設定単価（10万readsあたり0.03ドル、10万writesあたり0.09ドル）、1ドル150円とすると、追加管理費の利用相当額は0.0054円です。実測請求額ではなく、回数を置いた試算です。通信再試行、ルール評価の追加読み取り、保存・転送、通知側の費用はこの値には含みません。地域・契約単価による差があります。

10分間の条件別試算にも上記60 reads・20 writesを追加しました。2人全員が10分間発話・AI補助各100回・説明10回・新規たとえ10回・1ドル150円では、既存のAIと音声中継を含め約11.91〜12.06円。4人なら約23.19〜24.05円です。実際の会話での無言・共有済みの用語やたとえでは減ります。

今回の通話の概算は、番号機能で観測できた読み書きを接続・状態管理に加えます。画面移動前の発信・承認分はsessionStorageを経由して次の通話へ引き継ぎます。サーバールールの内部読み取り、通知・保存容量などを網羅する請求データではありません。切断時などに未反映になる可能性があります。
無料枠内なら対象の追加請求は0円です。無料枠残量や実際の請求額はこのアプリでは取得していません。Firestoreには保存・転送・ルール評価等の課金条件があります。[公式料金の説明](https://firebase.google.com/docs/firestore/pricing)。

## 確認結果
自動テスト94件成功。実サーバーで番号重複・一覧取得・未承認参加・5人目・ゲスト承認・退室後の無承認再参加を拒否することと、持ち主の承認を確認しました。
検証用の独立した3アカウントをブラウザー画面で操作し、初回応答で同じ部屋へ移動し、3人目の追加時に持ち主が移動せず同じ部屋へ承認されることを確認しました。この検証はマイクを使わない参加制御の確認です。iPhone実機での音声通話・閉じたアプリへの通知配信は今回未検証です。

## 次の段階
番号の引き継ぎ・本人のログイン、電話帳への番号登録、全ブロック一覧、複数端末の着信・通知を順に拡張できます。
