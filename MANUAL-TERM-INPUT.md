# 用語の手入力を通話状態から分離

更新：2026-09-03 / 2026-09-03-manual-terms

## 原因と変更

termInputはHTMLのdisabled属性に加え、通話未参加・マイク未準備の状態でJavaScriptから無効化されていた。「入力して調べられる」という案内に反して、参加前には入力できなかった。

入力欄と「調べる」ボタンを通話状態から切り離した。参加前、ミュート中、退室後でも入力して説明・たとえを取得できる。取得時は従来の匿名認証を使い、サーバーの認証や利用制限は維持する。認証待ちの間に検索を閉じた場合は、その後のAI要求を送らない。

手入力では音声認識を開始せず、入力した語を通話相手へ自動送信しない。たとえを取得した場合の共有保存は従来どおり。未参加のまま状態更新が発生しても、入力済みの結果が突然消えないよう修正した。参加後の退室では従来どおり用語・説明を消し、処理中の要求を中断する。

## 費用への影響

入力や文字の編集だけではAI料金は発生しない。「調べる」を押した場合だけ説明を取得する。参加前に検索できるようになった分、利用回数が増えれば費用も増えるが、1回あたりの計算は変更していない。

1ドル150円、基本説明の入力500・出力200トークンを仮定すると、新しい説明取得は約0.0165円/回。たとえの新規生成は入力800・出力600トークンで約0.039円/回。保存済みのたとえはAI再生成なし。同じ語の基本説明は端末で60秒再利用する。保存・取得等は無料枠内を仮定。実際のトークン数・為替・無料枠で変わるため、実請求額ではない。

自動検出や通話全体の料金試算は変更なし。有料契約も変更していない。実請求額や無料枠残量は取得していない。

## 確認

自動テスト86件成功。参加前の入力・説明取得・たとえ表示、未参加状態での画面維持、音声認識や相手への送信が発生しないこと、退室時の破棄を確認。

ブラウザーの試験画面で、通話に参加せず入力・検索できることを確認。実機iPhone Safariは未検証。
