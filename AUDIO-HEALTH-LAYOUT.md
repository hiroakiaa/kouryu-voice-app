# 音声状態バーの表示修正

更新日：2026-09-03

音声状態バー（audioHealth）が通話画面の縦方向レイアウトで縮み、内部の文字とメーターが下にはみ出す状態を修正しました。バーは高さを縮めず、内容に必要な高さを確保します。マイク操作は44px以上の高さを保ち、内部を中央にそろえました。480px以下ではノイズ状態を2行目に配置します。

変更はCSSによる見た目の調整です。マイク、音声認識、AI呼び出し、料金計算には変更ありません。この変更によるAI利用相当額の増加は0円です。実際の請求額は今回取得していません。

ブラウザのデモ通話で、PC幅のバー内に全要素が収まることを座標でも確認しました。390px幅でも表示を確認しました。実機のiPhoneでの確認は行っていません。
