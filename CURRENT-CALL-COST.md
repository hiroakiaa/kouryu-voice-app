# 今回の通話料金・履歴表示・退室の修正

更新日：2026-09-03

## 表示と仕組み

アプリ情報の appInfoCostSummary には今回の通話の概算金額だけ（約0.123円の形式）を表示します。料金モーダルは「今回の通話」を初期表示にし、音声認識、用語AI補助、短い説明、新しいたとえ、共有たとえの再利用、接続管理、中継通信の数字を表示します。「10分間の試算」は別タブで切り替えます。

音声認識に送った秒数とAI要求回数を各端末で数えます。共有済みのたとえという応答が返った場合は新規生成費を除きます。接続管理は既存の推定読み書き回数、中継はWebRTC統計のバイト数を使います。数値カウンターだけを既存の参加者間データチャネルで共有し、受け取った端末分を重複加算せず合算します。音声・認識文・用語を料金計測のために保存する処理はありません。退室後は金額を残し、次の参加でリセットします。ページを再読み込みすると今回のカウンターはリセットされます。

途中参加時には相手の参加開始からの先行利用を含みます。未更新の端末、未接続の相手、切断直前に届かなかった利用は未集計となり、全員の請求明細の合計ではありません。退室済みの相手から最後に受信した数値は今回の集計に残します。内訳の「参加分の受信済み集計」で集計対象数を示します。

## 履歴と退室の不具合

履歴が1件以上ある場合に、削除済みの formatCallCountRange を呼び出して描画が失敗していました。同じ履歴描画が退室処理にも含まれており、マイク・接続・ボタン更新まで進まなくなる経路がありました。不要な無料枠回数の表示と関数呼び出しを削除し、履歴モーダルとグラフが表示されるように修正しました。グラフは従来の接続管理費の履歴です。過去の記録にAI利用回数がないため、今回のAI込み概算と混同しない見出しを維持しています。

退室ではマイク停止と参加状態・ボタン更新を先に実行します。字幕の後処理や履歴描画が失敗しても、接続を閉じる処理を続けます。参加情報の送信が失敗してもローカルの退室処理を実行します。

## 費用と前提

料金計測のための新しいAI呼び出しやデータベース書き込みはありません。数値の共有で小さな通信量が増えます。中継を使う場合はその通信量も中継費の対象ですが、音声に比べて小さなデータです。実請求額は取得していません。

表示は無料枠適用前の利用相当額です。既存の単価を使い、説明は入力500・出力200トークン、たとえは800・600、用語補助は抽出800・200と審査600・400を各1回として計算します。実際のトークン数や審査の有無で差が出ます。失敗・中断した要求も処理された可能性があるため含めます。共有保存・Workers実行・静的配信は無料枠内を仮定しています。請求額や無料枠の残量を示すものではありません。

例：1ドル150円で、送信した認識音声60秒は約0.07694円、短い説明1要求は約0.0165円、新規たとえ1要求は約0.039円、用語補助1要求は約0.048円です。共有たとえ再利用では新規生成の0.039円を除きます。今回単価の再調査は行わず、従来の試算との整合性を保っています。

## 確認

89件の自動テストが成功。履歴2件からのグラフ描画、字幕・履歴処理の故障時もマイクと接続を停止すること、送信音声秒数の計測、複数端末の重複排除・再利用費の除外・退室後の保持・次回参加での初期化を検証しました。ブラウザで保存済み履歴を入れた確認用画面、料金タブの切り替え、390px幅の表示を確認しました。iPhone実機での操作は未確認です。
