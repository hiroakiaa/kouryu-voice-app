# AIによる専門用語候補の検出・共有辞書

更新：2026-09-03 / 公開版：2026-09-03-discovery

## 今回の変更

登録済みの辞書だけでは拾えなかった語を、AIが補助的に探すようにした。自動検出をONにすると使える。新しい語はAIが抽出・審査し、共有辞書に登録する。次の利用者はその語を辞書で検出できる。説明やジャンル別のたとえは従来どおり用語を押して取得する。

## 仕組み

1. 音声認識は既存のWhisper方式。既存辞書と共有辞書で語を検出し、先に一覧へ追加する。
2. 辞書で説明できない文字列が残っているとき、各端末で最大30秒に1回、最新の認識断片を最大300文字だけGemma 4へ送る。待機中の断片は新しい断片で上書きし、全文を蓄積しない。
3. AIが最大3語を抽出する。原文に含まれない語、形式に合わない語をプログラムで除外する。
4. 未登録の候補だけを、認識文を付けずに再度AIへ送り、一般的な専門用語かを審査する。短い一般的定義も生成できる語だけ登録する。審査用の定義は保存しない。
5. 共有辞書はSQLite Durable Objectsに最大1,000語保存する。重複をまとめ、上限では古い登録から外す。利用者ID・認識文・音声・出現履歴は保存しない。
6. 相手から届いた新しい語も共有辞書と照合する。相手が送った任意の文字列をそのまま採用しない。

AI補助の失敗時は辞書検出と通話を継続する。ミュート・自動検出OFF・退室で端末の待機断片と要求を破棄し、遅い結果は画面へ追加しない。すでにサーバーが受理した処理の完了・辞書登録まで取り消せるとは限らない。

メールアドレス・URL・長い数字列は送信前とサーバー側で除去する。これは匿名化の保証ではなく、人名等を含む認識断片がAIへ送られる可能性はある。登録審査では人名・会社名・地名・商品名・組織固有の呼称・日常語・造語を除外するよう指示している。

## 利用上の限界

30秒間の全文をすべて検査する機能ではない。最新の短い断片に絞るため、発話の間隔・認識結果の区切りによって拾えない語がある。新しい語の追加は音声認識時間に加え、最大約30秒の待ち時間とAI処理時間がかかる。登録済みの語は従来どおり認識結果の到着後に辞書で拾う。

2回目の確認も同じAIモデルによる分類であり、専門家や外部の辞典による事実確認ではない。検出漏れや誤登録が起きる可能性がある。手入力で調べる機能も引き続き利用できる。

共有辞書を使っても、別の未知候補や普通の漢字語が文に残ればAI抽出が実行される。「登録後はその語のためのAI審査が不要」という意味であり、その後の会話すべてのAI補助料金が0になるわけではない。

## 追加費用の概算

1ドル150円を仮定。Gemma 4は入力100万トークン0.10ドル、出力100万トークン0.30ドル。抽出は入力800・出力200トークン、審査は入力600・出力400トークンで見積もる。出力は実装でそれぞれ200・400に制限するが、入力トークン数は文章で変わるため厳密な料金上限ではない。

| 処理 | 1回の利用相当額 |
|---|---:|
| 短い認識文から候補抽出 | 約0.021円 |
| 未登録候補をまとめて審査 | 約0.027円 |
| 両方を実行 | 約0.048円 |

10分間に端末ごと20回、毎回未登録候補が出て審査も実行する想定では、追加分は1端末約0.96円、2端末約1.92円、4端末約3.84円。既知の語だけで辞書検出できれば0円、抽出のみ20回なら1端末約0.42円。自動検出OFF・無音・ミュート時の新しいAI補助要求はない。

## 10分間の通話全体

従来と同じ条件：全参加者の音声を10分間認識、基本説明は全体で10回、たとえは全体で10回。Firestoreは読み取り1,000・書き込み200・削除20回、TURNは直接接続〜全組み合わせ両端中継（片方向64kbps）を仮定。共有辞書・共有たとえの保存と要求、Workers実行、GitHub Pagesは無料枠内として0円とする。

| 条件 | 2人・10分 | 4人・10分 |
|---|---:|---:|
| AI補助なし・たとえ10件とも保存済み | 約1.78〜1.92円 | 約3.31〜4.18円 |
| AI補助を上記の頻度で抽出＋審査・たとえは保存済み | 約3.70〜3.84円 | 約7.15〜8.02円 |
| AI補助を上記の頻度で抽出＋審査・たとえ10件とも新規 | 約4.09〜4.23円 | 約7.54〜8.41円 |

これらは無料枠を除いた従量利用相当額で、請求額・課金上限ではない。実際には無音、候補の有無、トークン数、話者数、TURN経路で変わる。携帯回線の通信料は含まない。

現在のWorkers Freeを維持し、有料契約への変更はしていない。Workers AIの無料枠内では追加請求0円。音声認識・基本説明・たとえ・今回のAI補助はアカウント全体の10,000ニューロン/日を共有するため、今回の追加で無料枠を使い切るまでの時間は短くなり得る。Freeの枠超過時はAI処理が失敗する。FirestoreやTURNの請求は別の無料枠・アカウント利用量で決まる。今回、請求書の実額確認はしていない。

## 確認結果

- 自動テスト80件成功。認証・サイズ・頻度制限、原文にない候補の除外、語だけの再審査、保存内容、辞書の上限と復元、停止後の応答破棄、共有語の受信照合を確認。
- 本番APIで「冪等性・gRPC・イベントソーシング」を抽出・共有登録。処理約3.43秒、別の一時利用者から辞書取得約36ms（試験時の値）。
- 普通の昼食・休息についての試験文では追加0語。
- 実際の画面モジュールと本番APIを接続し、登録済みの語をAI補助要求0回で表示。新しい「ブルームフィルター」の追加も確認。
- 今回の画面試験は合成認識文を使用。実マイクや実機iPhone Safariによる音声からの一連の試験は行っていない。
- 一時テストアカウントを削除。検証に使った一般的な専門用語は共有辞書に残し、再利用可能。

## 料金の根拠

- [Cloudflare Workers AI](https://developers.cloudflare.com/workers-ai/platform/pricing/)
- [Cloudflare Durable Objects](https://developers.cloudflare.com/durable-objects/platform/pricing/)
- [Cloudflare TURN](https://developers.cloudflare.com/realtime/turn/faq/)
- [Firestore](https://cloud.google.com/firestore/pricing)
