# 理解支援通話の実装状況

2026-09-03: 字幕基盤の試用版に、Phase 2（客観情報強調）、Phase 3（聞き逃し）、Phase 4（専門語候補の下線）を順番に実装・検証。実音声認識の端末対応は引き続き確認が必要。Phase 5以降のAI解説は未実装。

## 実装

- 字幕は初期OFF。通話参加後、本人がONにすると自分の発言を文字起こしし、接続中の相手へ共有する。
- 端末内の日本語 SpeechRecognition のみ。processLocally=trueを必須とし、クラウドへの暗黙フォールバックを禁止。
- 対応するブラウザーでは、日本語モデルが未導入なら本人の操作でダウンロードできる。
- 未対応端末は自分の字幕生成ができない。字幕ONなら他端末からの字幕を受信できる。各発話者が対応端末で字幕をONにする必要がある。
- 両端が対応を通知した場合は字幕用DataChannel（ordered=true、maxPacketLifeTime=5000）で再送できる。旧版には従来のDataChannelを使い、新規チャネルで旧版の参加状態通信を壊さない。Firestoreへ字幕は書かない。DataChannelがTURN経由の場合の通信量は増える。
- 発話者は接続先のIDとセッションに基づいて表示。受信文字数、メッセージ数、送信バッファを制限する。
- 60秒・最大80発言の端末メモリのみ。認識器も30秒ごとに更新し、長時間の結果リスト蓄積を避ける。
- ミュートで認識中止、解除で再開。字幕OFF・退室・ページ離脱で認識、タイマー、字幕を破棄する。
- モジュールは動的に読み込む。読み込み失敗時も電話を初期化できる。CAPTIONS_ENABLED=falseで追加UIと処理を無効化可能。
- 日時・金額・数量・電話番号・期限をローカル分類で強調する。原文は変えず、本人が強調をOFFにできる。
- 「聞き逃した」は開いた時点の確定字幕だけを表示。発言IDだけを選択して参照し、60秒を過ぎた本文は表示中でも消える。他参加者への通知はない。
- 専門語候補は軽量辞書で検出。確定字幕のみ、1発言あたり最大4語・同じ語は1回。複合語を優先し、英字語の途中には下線を付けない。
- 下線タップは選択語と字幕原文の表示のみ。AI解説・例え・Web検索・外部API呼び出しはまだ行わない。原文カードも期限切れ、字幕OFF、退室で消える。
- CAPTION_FEATURESのfacts/replay/termsを個別に無効化可能。強調と下線は本人が画面上でも切り替え可能。設定や操作を他参加者へ送信しない。

## 検証済み

- Nodeの既存34件＋字幕12件＋通信互換性2件、計48件のテスト。
- ブラウザーで合成データを使用し4人・6組の実際のWebRTC DataChannel接続、各画面で全発話者の受信、全員退室時の消去。
- 本体と同じCSS・マークアップ・モジュールを使うiframeで幅390pxを検証。scrollWidth=clientWidth=390で横はみ出しなし。強調のON/OFF、用語タップ、聞き逃し表示、退室時の消去をブラウザー操作で確認。
- Codex内ブラウザーに日本語認識モデルをインストールできた。公開日本語音声をAudioContextから音声トラックとして入力する試験はabortedで中断したため、実音声認識の成功とは扱わない。マイクは使っていない。

## 未完了のリリース条件

- PC・iPhone・iPadを含む2台/4台の実音声認識、同時発話、エコー、遅延、ミュート、退室後のマイク表示の実機検証。
- 物理的なiPhone/iPadでのレイアウト・タッチ操作検証。390pxのブラウザー内検証は実機の代替とは扱わない。
- 実ネットワーク劣化時の字幕評価。新チャネルも5秒の期限を超えればパケットを破棄する。旧版へのフォールバックでは再送しない。
- iOS等の未対応端末に対する、日本語ローカル認識の代替方式または非保存条件を確認した外部ASRの設計。

字幕データに対するPhase 2〜4の処理は検証済みだが、サービス全体の字幕対応完了は宣言しない。
tests/caption-preview.htmlは合成データの4人通信検証、tests/caption-ui.htmlは固定テキストのUI検証。音声認識や物理実機通話の成功証拠ではない。

## 次の実装順

1. 字幕認識の対応環境と音声入力方式を引き続き検証。
2. Phase 5 タップ時だけの解説。利用可能なAIサービス・サーバー認証・非保存条件を確認し、APIキーをクライアントへ置かず構築する。
3. Phase 6以降の例え、個人設定を1段階ずつ。

参考仕様: https://developer.mozilla.org/en-US/docs/Web/API/SpeechRecognition/processLocally
