# 字幕・理解支援の実装と検証

2026-09-03 / `2026-09-03-streaming-captions-explanations`

## 今回の更新

- 通常参加・電話帳からの参加とも、参加直後はマイクON。
- 参加ボタンをコンテンツ領域いっぱいに表示。640px以上で親要素が縮む問題も修正。
- 数秒分まとめてから認識する方式から、100msごとに送信するストリーミング認識へ変更。途中字幕を受信次第WebRTC DataChannelで相手にも送り、同じ発言を確定結果で更新する。
- Phase 5「ひとことで」を追加。確定字幕の下線を押したときだけ、その語の一般的な意味を短く説明する。選んだ語だけを解説APIへ送り、説明は本人だけに表示。

## 音声認識

caption-stream.js と caption-stream-pcm.js が既存マイクからAudioWorkletで16kHzモノラルPCMを取り出す。ブラウザー内蔵SpeechRecognitionや端末内日本語モデルには依存しない。

- 字幕ON時だけ自分の音声を送信。ミュート・字幕OFF・退室で接続と処理を停止し、遅い結果を無視する。通話マイクのtrack.stop()は字幕から呼ばない。
- Cloudflare Worker /stream がFirebase IDトークンを検証してWorkers AIのNova-3へ中継。トークンはURLに入れない。
- 日本語の途中結果を順次配信。認識器の無音判定を利用し、空の確定結果は表示しない。「ご視聴ありがとうございました」「ご視聴ありがとうございます」だけの結果も抑制する。実際にこの定型句だけを話した場合も抑制される。
- 起動待ち15秒。送信詰まり・中断・障害時は停止理由を表示。接続は最長10分またはトークン期限までで更新し、短時間の再接続回数も制限する。
- 旧版向け /transcribe は互換性のため維持。旧版のWhisper用エネルギー判定はバッチ経路専用。

## 用語解説と非保存

- /explain は認証・Origin・入力サイズ・語の形式を検証。利用者ごと10回/分まで。
- Gemma 4に語の一般的な意味を依頼する。WebRTC等の一部には公開の基礎説明を添える。会話文脈の送信・会話の要約・意図や感情の推測は行わない。
- 解説は15秒で待機を打ち切り、失敗時に再試行ボタンを表示。カードを閉じる、字幕OFF、退室、原文の60秒期限で回答と処理を破棄する。
- 音声・字幕・選択語・回答のDB／ファイル保存や内容ログは実装しない。WorkerのObservability・Logpushを無効化し、AI Gatewayも使用しない。解説はstore:false、HTTP応答はno-store。
- 字幕は直近60秒・最大80発言だけ端末メモリに保持。Firestoreに送らずDataChannelで共有する。日時・金額・数量の強調、原文を読み返す「聞き逃した」、専門語候補の下線を維持。
- Phase 6以降の例え、ジャンル設定、Web検索等は未実装。

## 費用と利用条件

音声認識と用語解説はWorkers AIの利用枠・料金対象。Nova-3の公開単価は音声1分あたり0.0092米ドルで、接続中の無音音声も送信する。4人が字幕ONにすると4本分の音声処理になる。従来のWhisperより運営費が高くなるため、画面のFirestore／TURN概算にAI利用料を含まないことを明記した。有料プランへの変更はしていない。無料枠・上限に達すると字幕や解説が停止し得るが、通話は継続する。レート制限は厳密な課金上限ではない。

- Nova-3: https://developers.cloudflare.com/workers-ai/models/nova-3/
- Gemma 4: https://developers.cloudflare.com/workers-ai/models/gemma-4-26b-a4b-it/
- 料金: https://developers.cloudflare.com/workers-ai/platform/pricing/
- データ利用方針: https://developers.cloudflare.com/workers-ai/platform/data-usage/

## 検証と限界

- 自動テスト59件成功。音声形式変換、認証、入力制限、途中・確定字幕、停止後の結果破棄、タップ時だけの解説問い合わせ、閉じた後の回答破棄、既存通話機能の検査を含む。
- 公開日本語音声で本番ストリーミングAPIを検証。音声送信開始から最初の結果まで約1.5秒。
- ブラウザーのMediaStreamに公開音声を流し、本体モジュールと実際のWebRTC DataChannelで相手役へ送信。最初の字幕受信まで約2.0秒。1ブラウザー内の試験値であり、速度の保証ではない。
- 本番APIへ10秒の無音を送り、文字を含む字幕が返らないことを確認。
- 下線のWebRTCを押して短い解説が表示され、退室で原文・解説が消えることをブラウザーで確認。一時テストアカウントは削除。
- 通話全体のマークアップで390×700、390×550、640×800、900×800を確認。参加ボタンがコンテンツ幅と一致し、字幕30件の自動スクロール後は末尾まで0.5px以内。画面下端までスクロールしたときに通話ボタンが隠れない。
- 物理的なiPhone/iPad・Safari、複数実端末のマイク、エコー・同時発話・画面ロック・長時間通話は未検証。全端末・全ブラウザーの動作保証ではなく、HTTPS、マイク許可、WebRTC・AudioWorklet対応ブラウザーが前提。
- 認識には誤字・遅延があり、AI解説にも誤りがあり得る。各発言者が自分の字幕をONにする必要がある。
