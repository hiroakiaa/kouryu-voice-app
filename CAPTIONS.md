# 会話の用語一覧：実装と費用比較

2026-09-03 / 2026-09-03-term-assist-economy

## 変更した動作

- 全文字幕と「聞き逃した」を通常画面から外し、会話に登場した専門用語だけを一覧化。
- 本人が「自動検出をON」にしたときだけ、その人のマイク音声をCloudflareで認識。既存AudioWorkletで無音区間を省き、3〜8秒程度の音声区間をWhisper large-v3-turboへ送る。Nova-3の常時ストリーミングは新しい画面では使用しない。
- 一時的な認識文からブラウザー内の辞書で候補を抽出。全文は画面にも相手にも送らず、認識文を一覧や履歴に保存しない。認識サービスには用語を探すために音声を送るので、検出だけでもAI使用量は発生する。
- 検出した語だけをWebRTC DataChannelで共有。同じ語をまとめ、最大80語を端末メモリで保持し、上限では古い語から削除。自動検出OFF・退室で用語と解説を消去。会話文の「60秒」から、用語単体の「通話中だけ」へ保持ルールを変更したことを画面に明記。
- 語を押したときだけGemma 4で短い一般的な説明を取得。説明は本人だけに表示し、60秒で消去。同じ語を60秒以内に開き直す場合はメモリから再表示し、再リクエストしない。退室・閉じる操作で待機中の要求を中断し、遅い回答を表示しない。
- 辞書にない語は手入力で調べられる。手入力した語は相手へ送らない。自動検出OFFでも手入力なら音声認識費用はかからず、解説だけの利用になる。
- ミュート時は自分の音声認識を停止。相手の用語は受信できる。音声認識が失敗しても通話を止めない。
- 音声・全文・用語・解説のDBやファイル保存、内容ログは追加していない。自動検出は既存の認証済み /transcribe、解説は /explain を使用。バックエンドの有料契約変更はなし。

## どれくらい安くなるか

単に表示を用語だけにするのでは、音声認識料金は減らない。主な削減は音声認識モデルの変更による。解説は以前からクリック時だけ実行していた。

Cloudflare公式料金（2026-09-03確認）では、Nova-3 WebSocketは音声1分836.36ニューロン、Whisper large-v3-turboは46.63ニューロン。1,000ニューロンあたり0.011米ドル。モデル紹介ページにはWhisper単価0.00051米ドル/分、総合表には丸めた0.0005米ドル/分とあるため、ここではニューロン数から計算した0.00051293米ドル/分を使用。

同じ音声送信量なら約94.4%削減（約18分の1）。さらに無音を送らない分が減るが、環境音・発話の割合によるので固定の削減率には含めない。

1米ドル=150円と仮定。全員がその時間ずっと音声を送った場合の、無料枠適用前の音声認識費用：

| 利用例 | 以前の高速字幕 | 今回の用語検出 |
|---|---:|---:|
| 1人×10分 | 約13.8円 | 約0.77円 |
| 2人×10分 | 約27.6円 | 約1.54円 |
| 4人×10分 | 約55.2円 | 約3.08円 |
| 4人×60分 | 約331.2円 | 約18.47円 |

実際には無音の多い参加者の送信量が減る。送信した短い区間には発話前後の余白が含まれる。通信費、Firestore、Workers、TURN、解説費用は上表に含まない。

解説はGemma 4の入力100万トークン0.10米ドル、出力100万トークン0.30米ドル。仮に1回の入力500・出力200トークンなら約0.017円、10回で約0.17円。実際の長さで変動する。

AI無料枠10,000ニューロン/日を今回の音声認識だけに全て使うと、合計約214分の送信音声に相当。4人全員が連続送信する場合は約54分の通話。無料枠はアカウント全体で共有し、解説や他のAI利用分は差し引く。先の画像の残り5,330ニューロンなら約114分の送信音声（4人連続なら約29分）が計算上の目安。画像取得後の消費は含まない。

無料プランは上限でAI処理が停止する。有料プランに自動変更する実装はない。料金表は見積もりであり、実際の請求額ではない。

- https://developers.cloudflare.com/workers-ai/platform/pricing/
- https://developers.cloudflare.com/workers-ai/models/whisper-large-v3-turbo/
- https://developers.cloudflare.com/workers-ai/models/gemma-4-26b-a4b-it/

## 検証と制限

- 自動テスト66件成功。候補抽出、重複排除、全文を共有しないこと、不正な受信語の除外、手入力、クリック時だけの解説、メモリ再表示、停止・退室後の遅い結果の破棄を含む。
- 公開日本語サンプルを本番の低価格音声認識APIで処理し正常応答を確認。一時テストアカウントは削除。
- ブラウザーのMediaStreamで公開音声を流し、実際のWebRTC DataChannelを通じて「クラウド」だけが相手役に届くことを確認。音声再生開始から初回到着まで約10.1秒。以前の約2秒の逐次字幕より遅い。実マイクや物理的な別端末の試験ではない。
- 「クラウド」の解説を本番APIから取得し、画面に表示。小さい画面で20語の一覧と通話操作が重ならず、解説パネルが画面内に収まることを確認。
- 既知の辞書に含まれる語だけを自動抽出する。すべての専門語を網羅しておらず、未知の語・発音の揺れ・音声区間の境界・小声・雑音による取りこぼしがある。辞書にない場合は手入力を使う。
- 各発言者が自分の自動検出をONにする。旧版の全文字幕が届いた場合は用語だけを抽出して表示するが、旧版には新形式の用語一覧は出ないため、参加者全員でページを開き直す必要がある。
- iPhone Safariなどの実機、複数実マイクの同時発話、長時間通話は未検証。HTTPS、マイク許可、AudioWorklet・WebRTC対応ブラウザーが前提。
