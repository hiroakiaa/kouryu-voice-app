# 字幕の実装と検証状況

2026-09-03 / `2026-09-03-captions-cloud-asr`

## 今回の変更

本番の端末内日本語SpeechRecognitionへの依存を廃止した。通話中の既存マイクからAudioWorkletで音声を取り出し、16kHz・モノラルPCMにそろえてCloudflare Workers AIのWhisperへ送る。iPhone Safari、PC Chrome等で同じ処理を使う。端末内日本語モデルのダウンロードは不要。

- 本人が字幕ONを押したときだけ自分の音声を送信する。送信先・共有範囲をON前に表示。
- 通話マイクONが必要。各発言者も字幕ONにする。他人の音声を本人に代わって外部へ送信しない。
- 発話前の無音を除き、0.25秒の先行音声を含め、3〜8秒の区切りで処理。無音は送らない。連続発話は8秒で分割する。
- 返ってきた確定字幕を表示・共有。文字入力のような途中字幕はサーバー方式では未対応。
- 同時送信は1件、待機は1区間まで。認識が追いつかない場合は停止理由を表示。リクエスト待ちは12秒まで。
- ミュート・字幕OFF・退室では送信を中断し、遅い結果を無視。通話用マイクのtrack.stop()は字幕処理から呼ばない。
- iOSで音声処理を開始できるよう、字幕／マイクのクリック直後にAudioContextを起動。音声処理が中断した場合は再開操作を表示。

## サーバーと非保存

字幕専用Workerはcaption-worker/。公開先は https://kouryu-captions.allinone-hiroakiaa.workers.dev/transcribe 。既存の通話通知・TURN用Workerとは別。

- Firebase IDトークンの署名・発行元・対象プロジェクト・期限を検証。
- Origin・メソッド・PCM WAV形式・8秒相当の最大サイズを検証。
- Cloudflareのレート制限を利用者単位25回/分、IP単位120回/分に設定。これは厳密な課金上限ではない。
- 日本語文字起こしのみ。翻訳・要約・感情推定を要求しない。
- 音声・字幕のDB／ファイル保存、内容ログ、AI Gatewayログは使用しない。Observability・Logpushは無効。応答はno-store。
- Cloudflareのデータ利用方針: https://developers.cloudflare.com/workers-ai/platform/data-usage/ 。コンテンツを学習・サービス改善に使わず、ストレージサービスを組み合わせた場合の保存について記載されている。本Workerにストレージのバインディングはない。
- 音声認識はWorkers AIの利用枠・料金の対象。利用枠超過や障害では字幕が停止するが通話は継続。 https://developers.cloudflare.com/workers-ai/platform/pricing/

## 継続する字幕機能

- DataChannelで参加者へ共有し、Firestoreには保存しない。旧版との通信互換性を維持。
- 直近60秒・最大80発言だけを端末メモリに保持。字幕OFF・退室で消去。
- 日時・金額・数量などの強調、直前の確定字幕を読む「聞き逃した」、専門語候補への下線と原文表示。
- 各操作は本人だけに表示。Phase 5以降のAI解説・例え・Web検索は未実装。

## 検証と限界

- 自動テスト53件。音声形式変換、先行無音除外、入力検証、上限、失敗時動作、停止後の遅い結果の破棄を含む。
- 公開日本語サンプル音声を本番Workerへ送り、認証・Whisper認識・応答を確認。修正後は一文全体が返り、サーバー処理は約3.5秒だった。一時作成したテストアカウントは削除。
- ブラウザーで公開音声をMediaStreamに流し、本体と同じAudioWorklet・認識器・字幕UIを使った表示・送信処理への受け渡しと退室時消去を確認。実マイクは使用していない。
- 既存の4人DataChannel試験は合成文字データを使用。390px UI試験はブラウザー内で実施。
- 物理的なiPhone/iPad、Safari、複数実端末のマイク、同時発話、エコー、画面ロック、長時間通話は未検証。全端末・全ブラウザーの動作保証はしていない。HTTPS、マイク許可、WebRTCとAudioWorkletを利用できる現行ブラウザーが前提。
- 認識には遅延・誤字がある。長い発話の分割境界では特に誤認識が起こり得る。
