# 2種類の通話モード

## 変更内容

発信確認画面で「理解サポートあり」または「通話だけ」を選べるようにした。着信画面には発信者が選んだ種類を表示し、応答した2台は同じ設定で通話へ入る。履歴にも通話の種類を保存する。

グループでは管理者が通話の種類を設定する。参加者は同じ設定で入室し、既存グループは従来どおり「理解サポートあり」として扱う。

設定画面で、発信時に最初から選ばれる種類を変更できる。実際に発信した種類も端末内へ保存し、次回の初期選択にする。履歴と「よく使う」から再発信する場合は、その相手との直近の種類を引き継ぎ、発信確認画面で変更できる。

電話画面の操作結果は4秒間表示してから滑らかに消す。画面上へ重ねて表示するため、通知が現れたり消えたりしても電話ボタンなどの配置は動かない。

設定画面の番号引き継ぎでは、安全状態とログイン状態を最初は隠し、見出し横の「？」で説明と一緒に開閉する。メールアドレスとパスワードの両方へ入力した場合だけ、保存・ログインなどの操作ボタンを表示する。

## 仕組み

- `support`: Whisperによる発話認識、用語検出、短い説明、たとえを利用する。
- `plain`: WebRTC音声通話だけを開始し、字幕用データチャネルとWorkers AIへの要求を開始しない。
- 発信要求、1対1通話、グループ、履歴へ `supportMode` を保存する。
- 通話URLにもモードを付け、保存された通話設定と一致する場合だけ参加できる。
- グループで誰かが通話中の場合は種類を変更できない。通話途中で参加者の設定が分かれることを防ぐ。
- 料金画面では今回の通話を従来どおり実測し、10分試算には「通話だけ」を追加する。
- 料金履歴には、通話の種類、1対1かグループか、人数、時間、接続方式と各機能の費用を端末内へ保存する。

## 費用への影響

「通話だけ」はWorkers AIを使用しないため、音声認識・用語検出・説明・たとえの利用相当額は0円になる。Firestoreの接続状態管理と、直接接続できない場合のTURN中継だけが残る。無料枠内なら実際の請求額は0円である。

通話終了後の「今回の通話」には、音声認識0秒・用語AI補助0回・説明0回・たとえ0回と表示する。料金履歴の合計は接続管理だけでなく、利用したAIとTURNを含む全機能の概算である。

表示額は無料枠を差し引く前の利用相当額であり、実際の請求額ではない。TURN使用量は接続経路により変動する。

一時メッセージはブラウザ内の表示処理だけなので、追加料金は発生しない。

番号引き継ぎ画面の開閉と入力判定もブラウザ内の表示処理だけなので、追加料金は発生しない。実際に保存・ログインを押した場合のFirebase Authenticationは、従来と同じ処理である。

## 確認項目

- 発信確認で2種類を選択できること
- 着信側に選択内容が表示されること
- 両端末が同じモードのURLへ移動すること
- 「通話だけ」で用語パネルとWorkers AI処理を開始しないこと
- 履歴と10分料金試算にモードが反映されること
- 旧グループと旧履歴を「理解サポートあり」として扱えること
- 前回の選択、履歴からの再発信、設定画面の既定値が反映されること
- 料金履歴で通話種類と1対1・グループを区別できること
