# 連絡機能の安全性と公開検査

## 今回の変更

- 連絡へ任意の期限を設定できる。期限を過ぎた未完了の連絡は優先して並び、期限表示で判別できる。
- Push通知に失敗した宛先だけを再送できる。成功済みの人へ同じ通知を重ねて送らない。
- 管理者メニューで「全員」「グループ・所属」「急ぎ」の利用可否を端末ごとに設定できる。
- 管理者パスワードを管理者画面から変更できる。平文は保存せずSHA-256ハッシュだけを端末に保存する。
- 初期化が9秒以上完了しないと、アプリ本体とは独立した復旧ボタンを表示する。更新確認と古い画面キャッシュの削除後に読み直す。
- 公開前は全テストを1ファイルずつ実行してからpushする。公開後はGitHub ActionsがChromiumで全タブ、番号ボタン、管理者メニューを操作し、JavaScript例外がないことを確認する。
- 送信者・受信者の2端末を模したテストで、受付から完了までの状態同期を確認する。

## 権限の範囲

送信設定は、学校から貸与する端末での誤操作を防ぐための端末単位の制御である。現在の管理者認証は端末内認証なので、組織全体の役職をサーバーで証明する仕組みではない。将来、複数管理者や校外端末を厳密に制限する場合は、管理者アカウントとサーバー側の権限判定を追加する。

## 費用

期限と端末内の利用設定、初期化復旧、パスワード変更、検査は通常利用時の外部API呼び出しを増やさないため、通話料金は増えない。期限は既存の連絡文書へ数値を1項目加えるだけで、通常は料金差を円単位で確認できない程度である。

通知再送時だけ、失敗した相手1人につきPush通知1回とFirestore書き込み最大2回が増える。無料枠適用前の利用相当額でも1人への1回の再送はおおむね0.001円未満で、実際の請求額はFirebaseとCloudflareの月間無料枠、再送回数、全体利用量によって0円になる場合がある。

## 確認方法

- `node tests/phone-complete.test.mjs`
- `node tests/static-contract.test.mjs`
- `node tests/notice-two-device.test.mjs`
- 公開後: `.github/workflows/public-browser-check.yml` が `tests/browser-smoke.mjs` を実行

