# 管理・信頼性・費用表示の設計

更新日: 2026-09-05

## 利用者に見える変更

- 初回案内に「アプリ利用者同士専用」「一般電話番号と緊急通報には発信できない」「サポートあり／通常通話」の違いを短く表示します。
- 動きを減らす端末設定と高コントラスト表示に追従します。キーボード操作時のフォーカス表示は残します。
- 端末チェックでは、通信・接続情報・マイク・中継・着信通知を自動確認し、発信、着信、拒否、取消、再発信、画面ロック通知、回線切替後の音声復帰を実機で確認できます。

## 管理者メニュー

管理者パスワードは端末内で照合し、通過状態をこの端末だけに保存します。「認証解除」で保存状態を削除できます。サーバー側認証への移行は、Workerの秘密情報を更新できる運用経路を整えてから行います。

料金画面では、今回の通話、10分試算、履歴に加え、管理者が月の目安上限を設定できます。今月の合計は、この端末に残る料金履歴から計算します。全利用者・Cloudflare・Firebaseの実請求額ではありません。

## 障害記録と復旧

接続段階、マイク、WebRTC、TURN、Firestore listener、通知の成否は、名前や会話本文を含めず診断IDと時刻を端末内へ記録します。一般利用者向けの診断パネルは表示しません。通話終了時に listener、timer、peer、マイクが停止したかも記録します。

発信失敗、マイク拒否、満員、相手待ち、再接続、通知失敗は原因別の案内を表示します。通知は着信ID単位で重複を抑え、取消通知と有効期限で古い着信を残さない設計です。

## データの保存と引き継ぎ

匿名利用中の番号は端末に依存します。設定でメールアドレスとパスワードを登録すると、同じアカウントで番号、電話帳、履歴、グループを復元できます。音声と認識文は保存しません。共有用語の説明とたとえは、同じ内容を毎回AI生成しないため共有辞書へ保存します。

## 費用への影響

- 管理者ログインと確認: Cloudflare Workers のリクエストが、管理画面を開くときに1回ずつ増えます。通常利用では発生しません。Workers無料枠内なら実請求0円です。
- 月額上限表示、アクセシビリティ、初回案内、端末内診断: 外部APIを呼ばないため追加費用0円です。
- 通話、音声認識、AI用語検出、説明、たとえ、中継の計算方法は変更していません。

料金表示は無料枠適用前の利用相当額です。実請求額は各サービスの無料枠、他用途の使用量、為替、単価改定で変わります。

## 確認項目

自動確認ではHTML/JavaScript構文、root版とvoice-standalone版の一致、主要DOM IDの重複、電話状態遷移、通知取消と期限、料金計算を確認します。物理的に別のiPhoneとPCを使う発着信、画面ロック通知、Wi-Fi切替は自動化できないため、端末チェックの実機項目で最終確認します。


## 2026-09-05 着信応答表示の修正

着信へ応答して通話URLへ移動した直後から、参加ボタンを「応答を準備中…」の回転表示にします。初期化時の重複した事前権限確認を外し、実際の参加処理内で通話ルーム、メンバー、期限、通話種類を確認します。成功後は「退室する」へ切り替わり、失敗時は再操作できる案内へ戻ります。外部APIの呼び出し回数と料金計算は増えません。

## 2026-09-05 着信直後の自動参加再確認

着信側が通話ルームを作成して画面を移動した直後は、Firestoreから新しいルームを読み取れるまで短い時間差が生じる場合があります。自動参加ではローディングを維持し、0、0.3、0.5、0.8、1.2、1.6秒の間隔で参加資格を再確認します。確認できた時点で参加登録とマイク接続へ進みます。手動参加では従来どおり1回だけ確認します。再確認は既存のFirestore読み取りを最大5回増やすため、失敗が起きた着信1回あたりの利用相当額はごく小さく、無料枠内では実請求0円です。

## 2026-09-05 前回通話終了との競合修正

着信への応答時に前回の通話中情報が数秒だけ残っていた場合、直ちに失敗表示へせず、応答ボタンを「接続中…」にしたまま最大約4.2秒再確認します。古い通話の終了が反映されたら、新しい1対1ルームを作ってそのまま接続します。実際に別通話が続いている場合は、再確認後も接続しません。再確認が最大まで行われた場合はFirestore readsが最大約25回増える可能性があります。単価をreads 10万回あたり0.03米ドル、1ドル158円と仮定すると利用相当額は約0.0012円で、無料枠内なら実請求0円です。

## 2026-09-05 通話中アンテナのアニメーション

通話が接続できたときだけ、接続表示の3本のバーがそれぞれ異なる周期で滑らかに伸び縮みします。待機、接続処理中、再接続中、失敗時は動かさず、状態ごとの従来アイコンを表示します。端末で「視差効果を減らす」「動きを減らす」が有効な場合は静止表示になります。CSSだけで描画するため通信や外部APIは増えず、追加費用は0円です。

## 2026-09-06 会話中表示と応答後の高速接続

相手と自分の参加者アイコンに出る3本の会話中表示も、各バーが異なる周期で滑らかに伸び縮みします。端末の「動きを減らす」設定では静止します。この表示はCSSだけで動くため追加の通信やAPI料金はありません。

着信への応答後は、Cloudflare TURNの接続情報取得、匿名認証、参加資格確認を可能な範囲で並行して進めます。TURN取得が2.5秒を超えた場合はSTUNによる直接接続を先に試し、必要な参加処理を長時間止めません。また、通話ルーム作成直後の反映待ちは最大約4.4秒から約1.25秒へ短縮しました。既存の確認回数も6回から5回へ減るため、Firestoreの利用相当額はわずかに減ります。無料枠内であれば実請求は0円です。回線やマイク許可の状態によって接続時間は変わりますが、アプリ側の固定待機は最長約3秒以上短くなり、TURN応答待ちは最大2.5秒に制限されます。

## 2026-09-06 応答直後の接続を優先

応答処理で通話ルームを作成できたら、履歴保存の完了を待たずに通話画面へ進みます。履歴は画面移動までの間も非同期で保存し、通話情報はセッション内にも保持します。1対1通話では、応答時点ですでに2人分の通話資格と利用中情報を確保しているため、4人用の固定参加枠を4件読み取る処理を省きます。また、同じ通話IDの利用中情報が確認できた場合は、同一内容をTransactionでもう一度書き直しません。これにより、応答後の画面待機と通話画面でのFirestore往復を減らします。通話1回あたりのFirestore reads/writesも減るため追加料金はなく、利用相当額はわずかに下がります。

新しい着信へ利用者が応答した場合は、その操作を新しい通話開始の意思として扱い、前回の利用中記録を最大4.2秒再確認する固定待機を廃止しました。新しい通話ルームの作成と同じTransaction内で両者の利用中情報を新しい通話IDへ更新します。応答端末自身が通話中の場合は応答前に停止するため、同じ端末での二重参加は防ぎます。

応答後は別URLへページ全体を再読み込みせず、ブラウザの履歴だけを更新して同じ画面内で電話ホームから通話画面へ切り替えます。すでに完了しているFirebase認証、読み込んだJavaScript、着信監視を維持したまま、通話ルームの監視、マイク取得、WebRTC接続を開始します。これにより、iPhoneで再読み込みとFirebase再初期化にかかっていた約15秒の経路をなくします。通信回数は増えず、追加料金は0円です。

応答からの自動参加は、手動で参加ボタンを連打した場合の端末内回数制限には数えません。画面切り替え前の処理中状態が残っていても、応答経路ではそれを解除して自動参加を必ず開始します。これにより、応答後に「参加する」へ戻って手動操作が必要になる状態を防ぎます。サーバー上の通話資格、人数、期限の確認は従来どおり維持します。追加のAPI呼び出しや料金はありません。
