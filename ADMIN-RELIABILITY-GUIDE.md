# 管理・信頼性・費用表示の設計

更新日: 2026-09-05

## 利用者に見える変更

- 初回案内に「アプリ利用者同士専用」「一般電話番号と緊急通報には発信できない」「サポートあり／通常通話」の違いを短く表示します。
- 動きを減らす端末設定と高コントラスト表示に追従します。キーボード操作時のフォーカス表示は残します。
- 端末チェックでは、通信・接続情報・マイク・中継・着信通知を自動確認し、発信、着信、拒否、取消、再発信、画面ロック通知、回線切替後の音声復帰を実機で確認できます。

## 管理者メニュー

管理者パスワードは端末内で照合し、通過状態をこの端末だけに保存します。「認証解除」で保存状態を削除できます。サーバー側認証への移行は、Workerの秘密情報を更新できる運用経路を整えてから行います。

料金画面では、今回の通話、10分試算、履歴に加え、管理者が月の目安上限を設定できます。今月の合計は、この端末に残る料金履歴から計算します。全利用者・Cloudflare・Firebaseの実請求額ではありません。

## 障害記録と復旧

接続段階、マイク、WebRTC、TURN、Firestore listener、通知の成否は、名前や会話本文を含めず診断IDと時刻を端末内へ記録します。一般利用者向けの診断パネルは表示しません。通話終了時に listener、timer、peer、マイクが停止したかも記録します。

発信失敗、マイク拒否、満員、相手待ち、再接続、通知失敗は原因別の案内を表示します。通知は着信ID単位で重複を抑え、取消通知と有効期限で古い着信を残さない設計です。

## データの保存と引き継ぎ

匿名利用中の番号は端末に依存します。設定でメールアドレスとパスワードを登録すると、同じアカウントで番号、電話帳、履歴、グループを復元できます。音声と認識文は保存しません。共有用語の説明とたとえは、同じ内容を毎回AI生成しないため共有辞書へ保存します。

## 費用への影響

- 管理者ログインと確認: Cloudflare Workers のリクエストが、管理画面を開くときに1回ずつ増えます。通常利用では発生しません。Workers無料枠内なら実請求0円です。
- 月額上限表示、アクセシビリティ、初回案内、端末内診断: 外部APIを呼ばないため追加費用0円です。
- 通話、音声認識、AI用語検出、説明、たとえ、中継の計算方法は変更していません。

料金表示は無料枠適用前の利用相当額です。実請求額は各サービスの無料枠、他用途の使用量、為替、単価改定で変わります。

## 確認項目

自動確認ではHTML/JavaScript構文、root版とvoice-standalone版の一致、主要DOM IDの重複、電話状態遷移、通知取消と期限、料金計算を確認します。物理的に別のiPhoneとPCを使う発着信、画面ロック通知、Wi-Fi切替は自動化できないため、端末チェックの実機項目で最終確認します。


## 2026-09-05 着信応答表示の修正

着信へ応答して通話URLへ移動した直後から、参加ボタンを「応答を準備中…」の回転表示にします。初期化時の重複した事前権限確認を外し、実際の参加処理内で通話ルーム、メンバー、期限、通話種類を確認します。成功後は「退室する」へ切り替わり、失敗時は再操作できる案内へ戻ります。外部APIの呼び出し回数と料金計算は増えません。
