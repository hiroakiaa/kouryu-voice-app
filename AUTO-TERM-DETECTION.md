# 通話参加時の用語自動検出

更新：2026-09-03 / 2026-09-03-auto-terms

## 変更内容と仕組み

通話へ参加し、マイクが準備できると、用語検出を自動開始する。通常のON/OFFボタンは表示しない。

- ミュート中は自分の音声認識とAI補助の待機処理を停止する。相手から届く用語は引き続き受信・表示する。
- ミュート解除で自動再開する。状態更新のたびに認識処理を重複起動しない。
- 退室時は認識・待機中の要求を停止し、画面の用語と一時的な説明を消去する。再参加時は再び自動開始する。
- ブラウザーの音声開始制限や通信エラーでは「検出を再開」ボタンを表示する。開始待ちが8秒を超えた場合も再開操作を案内する。失敗時に自動で要求を繰り返し、利用量を増やし続けることはしない。
- 無音の送信抑制、AI補助の最大30秒間隔、説明・たとえのクリック時取得、共有辞書・たとえの再利用は維持する。

新しい版の利用者同士ではON操作は不要。旧版を開いたままの相手には再読み込みが必要。マイクの許可、通信状態、AI利用上限などによって検出できない場合はあり、常に成功することを保証する機能ではない。

## 費用への影響

料金画面の試算はすでに全参加者が10分間音声認識する前提なので、計算式は変更していない。1ドル150円、基本説明10回、各端末でAI補助の抽出・審査各20回、新規たとえ全体10回の条件では、2人で約4.09〜4.23円、4人で約7.54〜8.41円。これは無料枠適用前の利用相当額で、実請求額や上限ではない。

これまでOFFだった人も参加中は検出するため、実際の利用量と無料枠の消費は増える可能性がある。ON操作を省くこと自体の追加料金はない。ミュート中・退室後に新しい音声認識要求は送らない。停止前にサーバーで受理された処理の料金や保存を取り消せるとは限らない。

今回、有料契約への変更はしていない。Workers AIのFree枠内の追加請求は0円で、超過時はAI処理が停止する。Firestore等は別の無料枠・利用状況による。実際のアカウント残量や請求額は取得していない。

単価・計算条件の詳細は COST-DISPLAY.md、TERM-DISCOVERY.md を参照。

## 確認

自動テスト84件成功。参加時の一度だけの起動、ミュート中の停止と受信継続、解除での再開、退室・再参加、遅い応答の破棄、失敗時の明示的な再開を確認。

ブラウザーの音声試験画面で、参加状態になっただけで「検出中」になることを確認。実機iPhone Safariの試験は行っていない。
