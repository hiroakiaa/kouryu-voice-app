# 履歴からの再発信時の相手名修正

## 変更内容

履歴の「電話」から再発信した場合、履歴に保存されている相手名を発信確認画面と発信処理へ引き継ぐようにしました。相手が応答しない、拒否する、通話中などで発信が終了しても、履歴の表示名を電話番号で上書きしません。

## 仕組み

履歴の発信ボタンに番号と保存名を持たせます。発信時は電話帳の最新名を優先し、電話帳に一致がなければ履歴名を使用します。同じ名前を呼び出し中、応答済み、応答なしなどの状態更新で一貫して保存します。

## 費用への影響

画面と既存履歴データの扱いだけの変更です。API呼び出し回数、Firestoreの読み書き回数、Cloudflare・Workers AIの使用量は増えません。実際の追加請求額と無料枠を除いた利用相当額はいずれも **0円** です。

## 確認内容

履歴名が再発信処理に渡り、応答なしを含む終了状態でも同じ名前が保存されることを自動テストで確認します。ルート版と単独配布版のファイル一致も確認します。
