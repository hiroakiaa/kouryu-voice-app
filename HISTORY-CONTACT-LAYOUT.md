# 履歴・電話帳レイアウト改善

## 変更内容

- 登録済みの相手の履歴では「登録」ボタンを表示しません。
- 履歴画面の説明文を削除し、右上の検索アイコンで検索・全削除欄を開閉できるようにしました。
- 履歴カードの左端を人物アイコンに統一し、日時を月日と時分までにしました。
- 履歴上部に、通話回数と直近利用から選んだ「よく使う」相手を最大3件表示します。
- 電話帳が空の場合の説明文を削除しました。
- 電話帳追加フォームを初期状態では隠し、右下の＋ボタンから開閉できるようにしました。
- 電話帳を「あ行〜わ行」、A〜Z、数字、その他の順に分け、見出しと区切り線を表示します。
- 漢字名を正しい行に置くため、任意の「よみがな」欄を追加しました。

## 仕組み

「よく使う」は履歴を番号ごとに数え、回数が多い順、同数なら最近使った順で3件を選びます。履歴の登録ボタンは、同じ番号が電話帳に存在するかを端末上で判定します。

電話帳は、よみがながある場合はその先頭文字、ない場合は名前の先頭文字から行を決めます。カタカナも対応するひらがなの行へ変換します。漢字の読みはブラウザだけでは正確に判定できないため、よみがな未入力の漢字名は「その他」に入ります。

## 費用への影響

並べ替え、利用回数の集計、登録済み判定、開閉表示はすべて端末内で行います。よみがなは既存の電話帳レコードへ同時に保存するため、Firestoreの書き込み回数は増えません。AIは使用しません。今回の追加による実際の追加請求額と無料枠を除いた利用相当額はいずれも **0円** です。

## 確認内容

行の分類順、カタカナ変換、アルファベット順、よく使う3件、時刻の秒非表示、登録済みボタンの判定、開閉UI、Firestoreルール、公開用ファイルの一致を自動テストで確認します。
