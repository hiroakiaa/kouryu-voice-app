# 電話帳追加モーダル・履歴操作改善

## 変更内容

- 電話帳追加の＋ボタンを、アプリのクリーム色と緑色の枠・影に合わせ、電話帳画面の右下へ固定しました。
- 電話帳追加フォームを背景が暗くなるモーダルへ変更しました。右上の閉じるボタン、モーダル外のタップ、Escapeキーで閉じられます。
- 正常に登録・更新した後はモーダルを自動的に閉じます。
- 履歴カードの操作を「登録・削除・電話」の順に変更し、登録と削除にもアイコンを付けました。
- 画面幅540px以下では履歴操作の文字を隠し、分かりやすいアイコンだけを46pxの押しやすいボタンで表示します。読み上げ用の名前は残します。
- 「よく使う」カードの人物アイコンを大きくしました。

## 仕組み

電話帳フォームにはブラウザの `dialog` を使い、アプリ内のほかのモーダルと同じ背景、枠、閉じ方に統一しています。＋ボタンは電話帳一覧をスクロールしても画面右下に残ります。履歴ボタンはHTML上の並び順も変更し、狭い画面ではCSSで文字だけを非表示にします。

## 費用への影響

表示と操作だけの変更です。API、Firestore、Cloudflare、Workers AIの利用回数は増えません。実際の追加請求額と無料枠を除いた利用相当額はいずれも **0円** です。

## 確認内容

モーダルの開閉方法、操作順、各アイコン、540px以下の切り替え、右下固定、人物アイコンの大きさ、公開用ファイルの一致を自動テストで確認します。公開後は電話帳画面で＋ボタンとモーダル表示を確認します。
