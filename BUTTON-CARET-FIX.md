# ボタンのテキストカーソル修正

## 変更内容

ボタンを押そうとした時に文字入力用のカーソルや文字選択マーカーが出ないようにしました。通常ボタン、アイコンボタン、タブ、開閉項目の文字とアイコンへ共通で適用しています。入力欄では従来どおり文字カーソルを使用できます。

## 仕組みと費用

ボタン系要素の文字選択、iPhoneの長押しメニュー、入力キャレットをCSSで無効にし、マウスではポインターを表示します。通信処理は変更していないため、API利用量、実際の請求額、利用相当額への追加影響は0円です。

## 確認

共通CSSがボタンの子要素まで適用され、入力欄には適用されないことを自動テストで確認します。
