# たとえの共有辞典

公開版：2026-09-03-shared-analogies

同じ用語・ジャンルは最初の1回だけAI生成し、全利用者で再利用する。NFKC正規化・空白整理・小文字化で表記差をまとめる。ジャンルが違えば別のたとえを生成する。基本説明と音声認識の動作は従来どおり。

SQLite Durable Objectを用語・ジャンル・辞典バージョン単位で割り当てる。要求を直列処理して同時生成を防ぐ。回答の保存後に成功を返す。AIまたは保存の失敗時には成功として扱わず、AIへの直接フォールバックもしない。プロセス再起動後は永続保存から読む。障害でAI生成完了後の保存が失敗した場合まで厳密な「一生に一度」は保証しない。

保存するのは用語・ジャンル・回答・作成時刻・版・修正状態。会話・音声・利用者ID・閲覧履歴は保存しない。手入力の語もたとえ取得時には共有対象となることを画面に表示。基本説明は共有保存しない。端末表示は60秒で消え、共有辞典は残る。

「問題を報告して作り直す」で表示中の版を指定し、問題の回答を再利用対象から外して置換する。同じ旧版への再要求は新しい回答を返す。連続修正は5分間隔。修正失敗時も問題の回答は再配布せず、30秒後から再試行できる。他の端末ですでに表示している回答は最大60秒残る。既存の認証、入力検証、利用者10回/分制限も継続する。

## 費用

従来の仮定（入力800・出力600トークン、1ドル150円）では、たとえ1回の生成は約0.039円。同じ組み合わせを10人が1回ずつ取得すると、約0.39円から約0.039円へ、たとえのAI生成分は90%減る。100人なら99%減。別のジャンルや修正では新しい生成分が必要。

音声認識と基本説明は別料金なので、通話全体が90%安くなるわけではない。ANALOGIES.mdの10分試算で10件すべてが保存済みの場合、たとえ生成の約0.39円が不要となり、2人：約1.78〜1.92円、4人：約3.31〜4.18円。初めての10件なら従来の約2.17〜2.31円／約3.70〜4.57円。これは無料枠適用前の利用相当額。共有保存・読み取り・実行は現在のFree枠内と仮定し0円。実請求額や上限ではない。

Durable Objectsの無料枠は要求10万/日、実行13,000GB秒/日、読み取り500万行/日、書き込み10万行/日、保存5GB。Workers Freeのまま使用し、有料プランには変更していない。枠超過時は処理エラーとなる。

- [Cloudflare Durable Objects料金](https://developers.cloudflare.com/durable-objects/platform/pricing/)
- [Workers AI料金](https://developers.cloudflare.com/workers-ai/platform/pricing/)

## 確認

自動テストで複数利用者・表記差・同時アクセス・再起動後の再利用・旧版の修正要求・修正失敗時の隔離を確認。本番の2つの一時アカウントで同じ版を取得し、生成1件・共有1件を確認。初回約3.34秒、続く共有取得約52ms（試験時の値で速度保証ではない）。一時アカウントは削除済み。

390×550の画面でダイアログと閉じるボタンが画面内に収まることを確認。実機iPhone Safariは未検証。
