# 接続品質・電話帳・通知の改善

## 利用者に見える変更

電話帳の「よく使う」は、星を付けた相手を最優先にし、その後を通話回数、最後に直近の通話日時で並べます。最大3件です。電話帳の「…」メニューは、外側のクリックまたはEscキーで閉じます。ブロックは誤操作を避けるため、アプリ共通デザインの確認モーダルを表示します。

着信通知を有効にした端末は、アプリを表示したとき、通信がオンラインへ戻ったとき、表示中は15分ごとに通知登録を自動点検します。登録が古くなっていた場合は同じ端末の登録を作り直します。

「使い方のヒント」は一般利用者が操作に必要な説明へ絞りました。運営費、WebRTC、TURNなどの運営・通信診断情報は一般向けヒントから外し、管理者メニューにまとめています。

## 管理者メニュー

「今回の通話」の詳しい接続データ内に「接続品質」を追加しました。応答・参加から最初の音声までの秒数、直接接続またはTURN中継、自動再接続回数、未接続時に最後まで進んだ段階、各段階の所要時間を表示します。2秒以上かかった区間は色を変えます。

通話履歴には、接続秒数、再接続回数、停止地点、最大40件の接続タイムラインを端末内だけに保存します。名前、会話内容、通話URLは保存しません。これによりPC・iPhoneで繰り返し試した際、どの段階が遅かったかを比較できます。

## 動作確認

自動テストでは、PC・iPad・iPhone相当の参加・退室・再参加を10周、4端末通話を10周、電話帳の星優先順、メニューの閉じ方、ブロック確認、管理者向け接続品質表示、670px以下の横はみ出し防止を確認します。実際のPush通知到着と音声開始時間はApple端末・回線・通知サービスに依存するため、管理者メニューの記録を使って実機で最終確認できます。

## 費用と仕組み

画面表示、端末内の接続時刻記録、電話帳の並べ替え、メニュー操作には外部API料金はかかりません。通知登録の自動点検はアプリが表示されたときと15分ごとに行うため、Push登録APIへの小さな通信が増えます。通話の音声量、AI処理、Firestoreの通話データ、TURN中継の単価は変更していないので、10分通話の概算は従来と同じです。表示額は無料枠適用前の利用相当額であり、実際の請求額ではありません。
